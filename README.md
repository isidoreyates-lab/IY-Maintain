# IY Maintain

Production frontend for IY Maintain, deployed with GitHub Pages.

## Deployment
Upload/replace `index.html` in the GitHub Pages repository root. Keep the filename exactly `index.html`.

Supabase remains the live backend; do not replace or modify the backend project when deploying this frontend.
